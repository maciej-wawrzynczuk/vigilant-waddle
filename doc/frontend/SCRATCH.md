# Create a Frontend

Create a simple frontend. I want to load a portfolio and display it.

- Deploy in the local k8s cluster.
- Use nginx for static content and proxy.
- Make it minimal. Use simple CSS to keep it aesthetic.
  You can use external CSS and JS libraries, but download them.

Feel free to change my suggestion if it looks beneficial.
